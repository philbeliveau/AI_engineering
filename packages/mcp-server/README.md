# Knowledge MCP Server

MCP Server for AI Engineering knowledge base queries.

## Overview

This package handles:
- Real-time knowledge queries via MCP protocol
- Semantic search across knowledge base
- Extraction queries (decisions, patterns, warnings)
- Source management

## Development

```bash
uv sync
uv run python --version
```
