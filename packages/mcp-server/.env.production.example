# Knowledge MCP Server - Production Configuration
# Copy this file to .env and fill in REAL cloud credentials
#
# SECURITY WARNING:
# - NEVER commit .env files with real credentials to version control
# - Use secret management (Railway dashboard, AWS Secrets Manager, etc.) in production
# - Rotate credentials periodically

# ==============================================================================
# ENVIRONMENT
# ==============================================================================
ENVIRONMENT=production

# ==============================================================================
# MONGODB ATLAS (REQUIRED)
# ==============================================================================
# Get connection string from MongoDB Atlas dashboard:
# 1. Go to your cluster → Connect → Drivers → Python
# 2. Copy the connection string (mongodb+srv://...)
# 3. Replace <password> with your database user password
#
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://your_user:your_password@your-cluster.abc123.mongodb.net/knowledge_db?retryWrites=true&w=majority
MONGODB_DATABASE=knowledge_db

# ==============================================================================
# QDRANT CLOUD (REQUIRED)
# ==============================================================================
# Get credentials from Qdrant Cloud dashboard:
# 1. Go to your cluster → Access → API Keys
# 2. Copy the cluster URL and API key
#
# Format: https://<cluster-id>.<region>.cloud.qdrant.io:6333
QDRANT_URL=https://your-cluster.us-east1-0.cloud.qdrant.io:6333
QDRANT_API_KEY=your_qdrant_api_key_here

# ==============================================================================
# CONNECTION SETTINGS
# ==============================================================================
# SSL MUST be enabled for production cloud connections
SSL_ENABLED=true

# Increased timeout for cloud connections (network latency)
CONNECTION_TIMEOUT_MS=10000

# Higher pool size for production workload
MAX_POOL_SIZE=25

# ==============================================================================
# SERVER CONFIGURATION
# ==============================================================================
SERVER_HOST=0.0.0.0
SERVER_PORT=8000
LOG_LEVEL=INFO

# ==============================================================================
# PROJECT CONFIGURATION
# ==============================================================================
PROJECT_ID=default

# ==============================================================================
# AUTHENTICATION (REQUIRED FOR PRODUCTION)
# ==============================================================================
# Production MUST have API key authentication
API_KEYS_FILE=./api_keys.json
