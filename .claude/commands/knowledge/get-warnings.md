# Get Warnings and Anti-Patterns

Query the knowledge base for warnings, pitfalls, and anti-patterns to avoid.

Use the knowledge-pipeline MCP server's `get_warnings` tool to find warnings about: $ARGUMENTS

If no arguments provided, list all available warnings.

Present each warning with:
- The anti-pattern or pitfall
- Why it's problematic
- What to do instead
- Source attribution
