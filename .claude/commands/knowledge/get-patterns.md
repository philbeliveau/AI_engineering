# Get Implementation Patterns

Query the knowledge base for reusable implementation patterns.

Use the knowledge-pipeline MCP server's `get_patterns` tool to find patterns about: $ARGUMENTS

If no arguments provided, list all available patterns.

Present each pattern with:
- Pattern name
- Problem it solves
- Solution approach
- Code examples (if available)
- Trade-offs
- Source attribution
